// 기능 스위치.
//
// AI 제도 찾기는 기본 꺼짐이다. 만들어 두긴 했으나, 근거로 삼을 법령 정보가 아직
// 충분히 구조화되지 않았다고 판단했다 — 조문에 걸리지 않은 근거 법령이 남아 있고
// (예: 적격심사 제도에 "조달청 시설공사 적격심사 세부기준"이 연결돼 있지 않다),
// 별표도 18건만 확보했다. 이 상태로 답을 내보내면 "조문 대조 완료"라는 이 사이트의
// 신뢰를 깎는 쪽이 이득보다 크다.
//
// 되살리려면 CHAT_ENABLED=1 을 주면 된다. 코드·데이터·평가 하네스는 모두 남겨
// 두었으므로, 근거가 갖춰지는 대로 스위치만 올리면 된다.
//
// 주의: UI를 숨기는 것만으로는 부족하다. API가 그대로 열려 있으면 주소를 아는
// 사람은 계속 쓸 수 있고, 검증되지 않은 답이 나가는 것은 마찬가지다. 라우트에서도
// 같은 값을 보고 404를 돌려준다.
export const CHAT_ENABLED = process.env.CHAT_ENABLED === "1";
